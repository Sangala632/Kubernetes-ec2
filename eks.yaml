apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: Roboshop-Cluster
  region: us-east-1

managedNodeGroups:
  - name: Roboshop-NodeGroup
    instanceTypes: ["t3.medium"]
    desiredCapacity: 3
    spot: true

    volumeSize: 20